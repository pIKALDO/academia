# Infraestructura de desarrollo local para el backend.
# Uso: docker compose up -d
#
# Credenciales con valor por defecto pensado solo para desarrollo local; ninguna de estas
# variables se usa en servidor. Para cambiarlas sin tocar este fichero, crea un `.env` en la
# raíz del repositorio (ver `.env.example`).

services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      # Rol propietario: crea el esquema (vía Flyway) y es dueño de todas las tablas.
      POSTGRES_USER: ${POSTGRES_USER:-academia}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-academia_dev}
      POSTGRES_DB: ${POSTGRES_DB:-academia}
      # Rol de aplicación: sin propiedad sobre las tablas, es el único con el que se conecta
      # el backend en tiempo de ejecución. Lo crea backend/db/init/01-create-app-role.sh en
      # el primer arranque; sus privilegios los gestiona V6__app_role.sql.
      POSTGRES_APP_USER: ${POSTGRES_APP_USER:-academia_app}
      POSTGRES_APP_PASSWORD: ${POSTGRES_APP_PASSWORD:-academia_app_dev}
    ports:
      - "5432:5432"
    volumes:
      - academia_postgres_data:/var/lib/postgresql/data
      - ./backend/db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-academia} -d ${POSTGRES_DB:-academia}"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Almacenamiento S3-compatible para el desarrollo local. En servidor, el mismo código habla
  # con Cloudflare R2 (ver docs/modelo-datos.md sección 5.3 y backend/config/storage).
  minio:
    # MinIO retiró sus imágenes de Docker Hub; la distribución oficial ahora es quay.io.
    image: quay.io/minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-academia}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-academia_dev_minio}
    ports:
      - "9000:9000" # API S3
      - "9001:9001" # Consola web
    volumes:
      - academia_minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Crea el bucket de documentación al arrancar, si no existe ya: sin esto, la primera subida
  # fallaría con "bucket does not exist" en un entorno recién levantado. Contenedor de un solo
  # uso, no un servicio de fondo (sale en cuanto termina `mc mb`).
  createbuckets:
    # Igual que minio/minio: minio/mc también se retiró de Docker Hub, distribución en quay.io.
    image: quay.io/minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ${MINIO_ROOT_USER:-academia} ${MINIO_ROOT_PASSWORD:-academia_dev_minio};
      mc mb --ignore-existing local/${STORAGE_BUCKET:-academia-documents};
      "

  # Servidor SMTP de pruebas: captura los correos de avisos de caducidad sin enviarlos de
  # verdad. Interfaz web en http://localhost:8025.
  mailpit:
    image: axllent/mailpit:latest
    restart: unless-stopped
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Interfaz web

volumes:
  academia_postgres_data:
  academia_minio_data:
